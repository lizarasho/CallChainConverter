package expressions.types;


/**
 * Implementation of the {@code Expression} interface representing logical expressions.
 * Logical expressions are composed with the operators '|', '&', relational expressions and boolean constant {@code Bool}
 * The value of {@code LogicalExpression} can be only true or false.
 */
public interface LogicalExpression extends Expression {
}
